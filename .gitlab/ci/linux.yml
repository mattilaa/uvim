.linux:
  image: ubuntu:24.04
  before_script:
    - apt-get update -qq
    - DEBIAN_FRONTEND=noninteractive apt-get install -y --no-install-recommends build-essential cmake ninja-build git python3 python3-venv python3-pip ca-certificates

build:linux:
  extends: .linux
  stage: build
  script:
    - cmake -S . -B build -G Ninja -DCMAKE_BUILD_TYPE=Debug -DUVIM_BUILD_TESTS=ON -DUVIM_ENABLE_CLANGD_LSP=OFF
    - cmake --build build
  artifacts:
    paths:
      - build/
    expire_in: 1 hour

test:linux:unit:
  extends: .linux
  stage: test
  needs: ["build:linux"]
  script:
    - set -o pipefail
    - ctest --test-dir build --output-on-failure
        --output-junit Testing/junit.xml 2>&1
        | tee build/Testing/ctest.log
  artifacts:
    when: always
    paths:
      # Raw ctest stdout + the per-test logs ctest writes under Testing/
      - build/Testing/
    reports:
      junit: build/Testing/junit.xml
    expire_in: 1 week

test:linux:robot:
  extends: .linux
  stage: test
  needs: ["build:linux"]
  script:
    - python3 -m venv .venv
    - . .venv/bin/activate
    - python -m pip install --upgrade pip
    - python -m pip install -r tests/robot/requirements.txt
    - UVIM_BIN="$PWD/build/uvim" ./tests/robot/run_robot.sh tests/robot/
  artifacts:
    when: always
    paths:
      # output.xml, log.html, report.html, plus per-suite stdout
      - tests/robot/output/
    reports:
      junit: tests/robot/output/output.xml
    expire_in: 1 week
